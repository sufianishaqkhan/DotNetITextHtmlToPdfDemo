# DotNetITextHtmlToPdfDemo
In this demo project I have used the iTextSharp library to convert plain html to .pdf file. HTML5 rules and conventions must be followed for the html to be parsed correctly.
